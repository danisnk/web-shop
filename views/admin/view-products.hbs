<section>
  <div  class="container ">
    <table class="table mt-7" id="productsTable">
      <thead>
        <tr>
          <th scope="col">No.</th>
          <th scope="col">Title</th>
          <th scope="col">Cateogory</th>
          <th scope="col">Description</th>
          <th>Price</th>
          <th>Image</th>
          <th>Options</th>
        </tr>
      </thead>
      <tbody>
       
           {{#each products}}
          <tr>
            <th scope="row">1</th>
            <td>{{this.name}}</td>
            <td>{{this.category}}</td>
            <td>{{this.description}}</td>
            <td>₹ {{this.price}}</td>
            <td><img style="width: 50px;height: 50px" src="/product-images/{{this._id}}.jpg" alt=""></td>
            <td>
              <a href="/admin/edit-product/{{this._id}}" class="btn btn-primary">Edit</a>
            <a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Do you want to delete {{this.name}}')">Delete</a>
            </td>
            
          </tr>
          {{/each}}
        </table>
        <div class="row mt-2">
      <a href="/admin/add-product" class="btn btn-primary ml-auto md-auto ">Add Product</a>
    </div>
      </tbody>
    </table>
  </div>
</section>

<script>

  $(function () {
    $('#productsTable').DataTable();
} );
</script>